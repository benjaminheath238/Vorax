plugins {
    id('com.vorax.java-application-conventions')
}

group = 'com.vorax.server'
version = '1.0.0'

tasks.named('jar') {
    baseName('Vorax-Server')

    manifest {
        attributes "Main-Class": "com.vorax.server.Main"
    }
}
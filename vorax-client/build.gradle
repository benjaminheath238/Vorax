plugins {
    id 'com.vorax.java-application-conventions'
}

group = 'com.vorax.client'
version = '1.0.0'

tasks.named('jar') {
    baseName('Vorax-Client')

    manifest {
        attributes (
            "Main-Class": "com.vorax.client.Main"
        )
    }
}
plugins {
    id('com.vorax.java-application-conventions')
}

group = 'com.vorax'
version = '2.4.0'

tasks.named('jar') {
    baseName('Vorax')

    manifest {
        attributes 'Main-Class': "${project.group}.Main"
    }
}
plugins {
    id('com.vorax.java-application-conventions')
}

group = 'com.vorax'
version = '1.0.0'

tasks.named('jar') {
    baseName('Vorax')

    manifest {
        attributes "Main-Class": "com.vorax.Main"
    }
}